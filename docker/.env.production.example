# Dokploy Docker Compose Environment Variables
# Copy these to your Dokploy application's environment variables

# === REQUIRED: Database Configuration ===
MYSQL_ROOT_PASSWORD=your-secure-root-password-here
MYSQL_DATABASE=southeastarchers
MYSQL_USER=appuser
MYSQL_PASSWORD=your-secure-app-password-here

# === REQUIRED: Flask Configuration ===
FLASK_ENV=production
SECRET_KEY=your-very-secure-secret-key-minimum-32-characters-long

# === REQUIRED: Email (SMTP) Configuration ===
MAIL_SERVER=smtp.example.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-email-password
MAIL_DEFAULT_SENDER=noreply@southeastarchers.ie

# === REQUIRED: Sum Up Payment API ===
SUMUP_API_KEY=your-sumup-api-key-from-dashboard
SUMUP_MERCHANT_CODE=your-merchant-code

# === OPTIONAL: Port Configuration ===
# Change if port 5000 conflicts with other services
WEB_PORT=5000

# ============================================
# Notes:
# ============================================
# 1. Generate a secure SECRET_KEY:
#    python -c "import secrets; print(secrets.token_hex(32))"
#
# 2. Database credentials:
#    - Use strong, unique passwords
#    - MYSQL_PASSWORD is used by the app to connect
#    - MYSQL_ROOT_PASSWORD is for database admin
#
# 3. Redis: No configuration needed, runs with defaults
#
# 4. SSL/TLS: Set MAIL_USE_TLS=False if not using TLS
#
# 5. For development, see docker/.env.dev.example
